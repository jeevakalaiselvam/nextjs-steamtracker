import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  display: flex;
`;

const MenuItem = (props) => {
  return <Container>MenuItem</Container>;
};

export default MenuItem;
