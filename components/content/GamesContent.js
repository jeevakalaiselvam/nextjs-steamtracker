import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  padding: 1rem;
`;

const GamesContent = (props) => {
  return <Container>Games Content</Container>;
};

export default GamesContent;
